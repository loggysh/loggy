apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: loggy-ingress
spec:
  rules:
  - http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: loggy
            port:
              name: web
